<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= content_for?(:title) ? yield(:title) : "Old Folk Shop" %></title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render partial: "partials/facebooksdk" %>
    <%= render partial: "devise/registrations/new" %>
    <%= render partial: "devise/sessions/login" %>
    <%= render partial: "partials/header" %>

    <%= render partial: "partials/flash_messages" %>
    <!-- might be duplicated as we have flash_messages
    <% if notice.present? %>
    <div class="notice success callout" data-closable>
      <p><%= notice %></p>
      <button class="close-button" aria-label="Dismiss alert" type="button" data-close>
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
    <% end %>
    <% if alert.present? %>
    <div class="alert callout" data-closable>
      <p><%= alert %></p>
      <button class="close-button" aria-label="Dismiss alert" type="button" data-close>
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
    <% end %> -->

    <%= yield %>
    <%= render partial: "partials/footer" %>
  </body>
</html>
